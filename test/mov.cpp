#include<bits/stdc++.h>

using namespace std;

long func(long a, long b, long c, long d,
         long e, long f, long g, long h){
    long sum;
    sum = (a + b + c + d + e + f + g + h);
    return sum;
}

int main(){
    long sum;
    sum = func(1, 2, 3, 4, 5, 6, 7, 8);
    return 0;
}